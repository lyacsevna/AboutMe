import React from 'react';

const Contacts = () => {
    return (
        <section id="contacts">
            <h2>Contacts</h2>
            <a href = "https://github.com/lyacsevna">github</a> 
        </section>
    );
};

export default Contacts;